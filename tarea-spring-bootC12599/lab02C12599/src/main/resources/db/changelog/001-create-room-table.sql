CREATE TABLE ROOM_C12599 (
                             id BIGINT AUTO_INCREMENT PRIMARY KEY,
                             room_identifier VARCHAR(255) NOT NULL UNIQUE,
                             name VARCHAR(255) NOT NULL
);
